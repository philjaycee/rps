const router = require('express').Router()

// Controllers
const auth = require('./controllers/authoriController')

/*
router.get('/api/v1/auth/login')
router.post('/api/v1/auth/login', auth.login)
*/

module.exports = router;